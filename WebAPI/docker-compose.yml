services:
    api:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - 80:80
        restart: always
        volumes:
            - ./server:/app
            - /app/obj/
            - /app/bin/
            - /app/out/